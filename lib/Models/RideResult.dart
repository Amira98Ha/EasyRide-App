class RideResult {
  var app_name;
  var product_id;
  var display_name;
  var estimate_price;
  var estimate_time;

  RideResult(this.app_name, this.product_id, this.display_name, this.estimate_price,
      this.estimate_time);
}