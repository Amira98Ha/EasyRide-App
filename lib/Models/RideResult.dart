class RideResult {
  var app_name;
  var product_id;
  var display_name;
  var estimate_price;
  var estimate_time;
  var low_estimate;
  var high_estimate;

  RideResult(this.app_name, this.product_id, this.display_name, this.estimate_price,
      this.estimate_time, this.low_estimate, this.high_estimate);
}