import 'package:flutter/material.dart';

class FastestRideScreen  extends StatelessWidget {
  const FastestRideScreen ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is FastestRideScreen :)))"),
    );
  }
}
